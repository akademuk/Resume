# 🤖 Настройка Telegram Bot для живого чата

## Шаг 1: Создание Telegram Bot

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Придумайте имя для бота (например: "Oleg Portfolio Chat Bot")
4. Придумайте username (например: "oleg_portfolio_bot")
5. **BotFather** отправит вам **токен** вида: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Сохраните этот токен!**

## Шаг 2: Получение Chat ID

### Способ 1 (Простой):
1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему любое сообщение
3. Он ответит вашим **Chat ID** (например: `123456789`)

### Способ 2 (Через браузер):
1. Отправьте сообщение вашему новому боту (найдите его по username)
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
3. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID

## Шаг 3: Настройка кода

Откройте файл **`script_new.js`** и найдите строки (примерно 1095-1096):

```javascript
const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE'; // Замените на ваш токен
const TELEGRAM_CHAT_ID = 'YOUR_CHAT_ID_HERE'; // Замените на ваш Chat ID
```

Замените на ваши данные:

```javascript
const TELEGRAM_BOT_TOKEN = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz'; // Ваш токен от BotFather
const TELEGRAM_CHAT_ID = '123456789'; // Ваш Chat ID
```

## Шаг 4: Тестирование

1. Откройте сайт в браузере
2. Кликните на иконку **Чат** в dock
3. Напишите тестовое сообщение
4. Проверьте Telegram - должно прийти уведомление от бота!

## 📱 Как это работает?

```
Рекрутер пишет в чат на сайте
           ↓
Сообщение через Telegram Bot API
           ↓
Вы получаете в Telegram
           ↓
Отвечаете прямо из Telegram
           ↓
(Опционально: можно настроить webhook для автоматических ответов)
```

## 🔒 Безопасность

⚠️ **ВАЖНО:**
- Никогда не публикуйте токен бота в GitHub или публичных репозиториях!
- Для продакшена используйте переменные окружения
- Рассмотрите использование серверной части (Node.js/PHP) для защиты токена

## 🚀 Продвинутые возможности

### Двусторонний чат (требует сервер)

Для реального двустороннего чата вам понадобится:

1. **Webhook сервер** (Node.js/Express пример):

```javascript
// server.js
const express = require('express');
const TelegramBot = require('node-telegram-bot-api');

const bot = new TelegramBot('YOUR_BOT_TOKEN', { polling: true });
const app = express();

// Когда вы отвечаете в Telegram
bot.on('message', (msg) => {
    // Отправить сообщение на сайт через WebSocket или Server-Sent Events
    io.emit('new-message', {
        text: msg.text,
        time: new Date()
    });
});

app.listen(3000);
```

2. **WebSocket на клиенте** для получения ответов в реальном времени

## 📝 Примеры использования

### Базовая команда для бота:
```javascript
bot.onText(/\/start/, (msg) => {
    bot.sendMessage(msg.chat.id, 'Привет! Я бот Олега. Все сообщения с сайта будут приходить сюда.');
});
```

### Автоответ:
```javascript
bot.on('message', (msg) => {
    if (msg.text.includes('привет')) {
        bot.sendMessage(msg.chat.id, 'Здравствуйте! Олег скоро ответит.');
    }
});
```

## ❓ Проблемы?

**Сообщения не приходят?**
- Проверьте токен и Chat ID
- Убедитесь что написали боту хотя бы одно сообщение
- Откройте консоль браузера (F12) для просмотра ошибок

**Ошибка CORS?**
- Telegram API работает через HTTPS, убедитесь что сайт тоже на HTTPS
- Для локальной разработки это не критично

**Хотите больше функций?**
- Добавьте кнопки inline в Telegram
- Отправляйте файлы, изображения
- Настройте автоответы с AI (ChatGPT)

---

💡 **Совет:** Для профессионального использования рассмотрите готовые решения типа Telegram Web App или Tawk.to с Telegram интеграцией.

🎯 **Цель:** Ваш сайт-резюме теперь имеет живой чат! Рекрутеры могут написать вам напрямую, и вы получите сообщение в Telegram мгновенно.
